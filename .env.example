# Newton secrets — loaded by config.py, never committed to git
TELEGRAM_BOT_TOKEN=

# LLM provider (set whichever matches config.toml [llm] provider)
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=   # only needed when provider = "openrouter"

# Embeddings — set OPENAI_API_KEY to use OpenAI directly (recommended),
# or rely on OPENROUTER_API_KEY if you prefer to route through OpenRouter.
OPENAI_API_KEY=

# Logfire — set token to send OTEL traces to Logfire cloud
# Get a token at https://logfire.pydantic.dev
LOGFIRE_TOKEN=

# SearXNG — web search endpoint
SEARXNG_BASE_URL=

# Vikunja — self-hosted task/project management
VIKUNJA_BASE_URL=
VIKUNJA_API_TOKEN=
